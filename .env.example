# Supabase (frontend — safe to expose; publishable key only)
VITE_SUPABASE_URL=https://xhscpwtvvjalfpzfarqw.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx

# ---------------------------------------------------------------------------
# The following are SERVER-SIDE ONLY secrets. They are NEVER read by the
# frontend. Set them as Supabase Edge Function secrets, not in this file:
#
#   supabase secrets set ANTHROPIC_API_KEY=...
#   supabase secrets set ALPACA_API_KEY=...       (optional global default)
#   supabase secrets set ALPACA_API_SECRET=...     (optional global default)
#
# Per-user Alpaca keys are stored encrypted in Supabase Vault via the app.
# ---------------------------------------------------------------------------
